query projectUsage($projectId: ID!) {
  project(id: $projectId) {
    usage {
      jobCount
      jobDuration
      storageSize
    }
    usagePack {
      name
      jobDurationMonthly
      jobCountMonthly
      storageSize
    }
  }
}